import * as eks from "aws-cdk-lib/aws-eks";
import { Stack } from "aws-cdk-lib";
import { Construct } from "constructs";
export declare function setupClusterLogging(stack: Stack, cluster: eks.Cluster, enableLogTypes: string[]): void;
interface Tag {
    Key: string;
    Value: string;
}
/**
 * Creates the node termination tag for the ASG
 * @param scope
 * @param autoScalingGroup
 */
export declare function tagAsg(scope: Construct, autoScalingGroup: string, tags: Tag[]): void;
export {};
