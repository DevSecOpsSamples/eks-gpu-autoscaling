import { Cluster, ServiceAccount } from "aws-cdk-lib/aws-eks";
import { PolicyDocument, IManagedPolicy } from "aws-cdk-lib/aws-iam";
/**
 * Creates a service account that can access secrets
 * @param clusterInfo
 * @returns sa
 */
export declare function createServiceAccount(cluster: Cluster, name: string, namespace: string, policyDocument: PolicyDocument): ServiceAccount;
export declare function createServiceAccountWithPolicy(cluster: Cluster, name: string, namespace: string, policy: IManagedPolicy): ServiceAccount;
